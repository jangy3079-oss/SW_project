spring:
  datasource:
    url: jdbc:mysql://localhost:3306/donga_dating?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root          # TODO: 실제 DB 계정으로 변경
    password: pskwi0375#     # TODO: 실제 DB 비밀번호로 변경
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: yourgmail@gmail.com
    password: 앱비밀번호
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: validate    # schema.sql 로 직접 관리 → validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  # schema.sql 초기 실행 (처음 한 번만 실행하고 이후엔 false 로)
  sql:
    init:
      mode: never           # DB 최초 구축 시 always 로 변경 후 실행, 이후 never

  # 사진 업로드 크기 제한
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

# 사진 저장 경로 (로컬)
file:
  upload-dir: uploads/profiles

server:
  port: 8080

logging:
  level:
    com.donga.dating: DEBUG
    org.hibernate.SQL: DEBUG


